<div class="row">
    <h1>Actions</h1>

    <table id="records">
        <tr>

        <td>Type</td>
        <td>Price</td>
        <td>When</td>

        </tr>
        {{#each actions}}
        <tr>
            <th>{{data}}</th>
            <th>{{totalCost}}</th>
            <th>{{time}}</th>
        </tr>
        {{/each}}
    </table>
</div>